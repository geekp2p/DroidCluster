# Example environment configuration for DroidCluster

# Friendly name of the Android device exposed by the emulator
# or physical device. Used by the emulator container.
DEVICE=Samsung Galaxy S10

# Extra parameters passed to the emulator process
EMULATOR_PARAMS=-noaudio -gpu swiftshader_indirect

# TCP port on the host exposed by the PlayFlow service
PLAYFLOW_PORT=5000

# Optional device serial that PlayFlow should connect to.
# Leave empty to let PlayFlow auto-detect.
DEVICE_SERIAL=

# Override ADB server socket if needed
#ADB_SERVER_SOCKET=tcp:5037